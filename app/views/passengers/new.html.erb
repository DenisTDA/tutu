<h1>Новый пассажир</h1>

<%= form_for @passenger do |form| %>
  <% if @passenger.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@passenger.errors.count, "error") %> prohibited this passenger from being saved:</h2>

      <ul>
      <% @passenger.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :passenger_name %>
  </div>

  <div class="field">
    <%= form.label :lastname %>
    <%= form.text_field :lastname, id: :passenger_lastname %>
  </div>

  <div class="actions">
    <%= form.submit 'Создать'%>
  </div>
<% end %>

<%= link_to 'Назад', passengers_path %>
